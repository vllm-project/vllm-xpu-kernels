#pragma once

#include "paged_decode.hpp"

// =============================================================================
// Configuration: Add new policies here
// =============================================================================
// To add a new decode policy, define aliases to avoid comma issues in macros

// X-Macro list using aliases
#define PAGED_DECODE_POLICY_LIST(X) \
  X(decode_policy_q8_h64_p64)       \
  X(decode_policy_q8_h96_p64)       \
  X(decode_policy_q8_h128_p64)      \
  X(decode_policy_q8_h192_p64)      \
  X(decode_policy_q8_h256_p64)      \
  X(decode_policy_q16_h64_p64)      \
  X(decode_policy_q16_h96_p64)      \
  X(decode_policy_q16_h128_p64)     \
  X(decode_policy_q16_h192_p64)     \
  X(decode_policy_q16_h256_p64)     \
  X(decode_policy_q8_h64_p128)      \
  X(decode_policy_q8_h96_p128)      \
  X(decode_policy_q8_h128_p128)     \
  X(decode_policy_q8_h192_p128)     \
  X(decode_policy_q8_h256_p128)     \
  X(decode_policy_q16_h64_p128)     \
  X(decode_policy_q16_h96_p128)     \
  X(decode_policy_q16_h128_p128)    \
  X(decode_policy_q16_h192_p128)    \
  X(decode_policy_q16_h256_p128)

// =============================================================================
// Automatic extern template declarations for all policy combinations
// =============================================================================
// Prevent implicit instantiation of decode_policy_dispatch_impl in translation
// units that include paged_decode.hpp. Each specialization is explicitly
// instantiated in its own .cpp file generated by CMake.

// Helper macro to declare a single extern template instantiation
#define DECLARE_DECODE_DISPATCH_EXTERN(                   \
    POLICY, Q_T, KV_T, O_T, CAUSAL, LOCAL, SINK)          \
  extern template void decode_policy_dispatch_typed_impl< \
      POLICY,                                             \
      Q_T,                                                \
      KV_T,                                               \
      O_T,                                                \
      CAUSAL,                                             \
      LOCAL,                                              \
      SINK>(sycl::queue & queue, const paged_decode_args_t& args);

// Allowed dtype combinations (must match runtime dispatch constraints):
// 1) Q=half    -> O=half,    KV in {half, fp8_e4m3, fp8_e5m2}
// 2) Q=bf16    -> O=bf16,    KV in {bf16, fp8_e4m3, fp8_e5m2}
// 3) Q=fp8_e4m3-> O in {half,bf16}, KV=fp8_e4m3
// 4) Q=fp8_e5m2-> O in {half,bf16}, KV=fp8_e5m2
#define DECLARE_ALLOWED_DTYPES(POLICY, CAUSAL, LOCAL, SINK)                \
  DECLARE_DECODE_DISPATCH_EXTERN(                                          \
      POLICY, half_t, half_t, half_t, CAUSAL, LOCAL, SINK)                 \
  DECLARE_DECODE_DISPATCH_EXTERN(                                          \
      POLICY, half_t, float_e4m3_t, half_t, CAUSAL, LOCAL, SINK)           \
  DECLARE_DECODE_DISPATCH_EXTERN(                                          \
      POLICY, half_t, float_e5m2_t, half_t, CAUSAL, LOCAL, SINK)           \
  DECLARE_DECODE_DISPATCH_EXTERN(                                          \
      POLICY, bfloat16_t, bfloat16_t, bfloat16_t, CAUSAL, LOCAL, SINK)     \
  DECLARE_DECODE_DISPATCH_EXTERN(                                          \
      POLICY, bfloat16_t, float_e4m3_t, bfloat16_t, CAUSAL, LOCAL, SINK)   \
  DECLARE_DECODE_DISPATCH_EXTERN(                                          \
      POLICY, bfloat16_t, float_e5m2_t, bfloat16_t, CAUSAL, LOCAL, SINK)   \
  DECLARE_DECODE_DISPATCH_EXTERN(                                          \
      POLICY, float_e4m3_t, float_e4m3_t, half_t, CAUSAL, LOCAL, SINK)     \
  DECLARE_DECODE_DISPATCH_EXTERN(                                          \
      POLICY, float_e4m3_t, float_e4m3_t, bfloat16_t, CAUSAL, LOCAL, SINK) \
  DECLARE_DECODE_DISPATCH_EXTERN(                                          \
      POLICY, float_e5m2_t, float_e5m2_t, half_t, CAUSAL, LOCAL, SINK)     \
  DECLARE_DECODE_DISPATCH_EXTERN(                                          \
      POLICY, float_e5m2_t, float_e5m2_t, bfloat16_t, CAUSAL, LOCAL, SINK)

// Generate all 8 bool combinations for a given policy using nested macros
// Pattern: Causal, Local, Sink (all permutations of 3 bools = 2^3 = 8)
// This hierarchical approach makes it easy to extend to more bool parameters

// Level 3: Iterate over Sink values (innermost)
#define DECLARE_FOR_SINK(POLICY, CAUSAL, LOCAL)        \
  DECLARE_ALLOWED_DTYPES(POLICY, CAUSAL, LOCAL, false) \
  DECLARE_ALLOWED_DTYPES(POLICY, CAUSAL, LOCAL, true)

// Level 2: Iterate over Local values
#define DECLARE_FOR_LOCAL(POLICY, CAUSAL) \
  DECLARE_FOR_SINK(POLICY, CAUSAL, false) \
  DECLARE_FOR_SINK(POLICY, CAUSAL, true)

// Level 1: Iterate over Causal values (outermost)
#define DECLARE_ALL_BOOL_COMBINATIONS(POLICY) \
  DECLARE_FOR_LOCAL(POLICY, false)            \
  DECLARE_FOR_LOCAL(POLICY, true)

// Apply the bool combination generator to all policies
PAGED_DECODE_POLICY_LIST(DECLARE_ALL_BOOL_COMBINATIONS)

// Cleanup macros
#undef DECLARE_ALL_BOOL_COMBINATIONS
#undef DECLARE_FOR_LOCAL
#undef DECLARE_FOR_SINK
#undef DECLARE_ALLOWED_DTYPES
#undef DECLARE_DECODE_DISPATCH_EXTERN
