# =============================================================================
# Full sparse MLA kernel configuration
# =============================================================================
# Build all sparse MLA kernel instantiations.
all
